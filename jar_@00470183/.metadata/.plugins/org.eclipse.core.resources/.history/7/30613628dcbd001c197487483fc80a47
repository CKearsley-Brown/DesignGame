package aee926.sciencegame;

import java.util.ArrayList;

import javafx.scene.canvas.GraphicsContext;
import javafx.scene.shape.Rectangle;

abstract class Graphic {
	public abstract void display(GraphicsContext gc);
}

class Wall extends Graphic {
	public Wall()
	{
		Rectangle r = new Rectangle();
		r.setX(0);
		r.setY(0);
		r.setWidth(200);
		r.setHeight(100);
		r.setArcHeight(20);
		r.setArcHeight(20);
	}
	
	public void display(GraphicsContext gc) {
		
	}
	
}

class Walls extends Graphic {
	public ArrayList<Graphic> walls = new ArrayList<Graphic>();
	
	public Walls(GraphicsContext gc)
	{
		display(gc);
	}
	
	public void add(Graphic leaf)
	{
		walls.add(leaf);
	}
	
	public void display(GraphicsContext gc)
	{
		for(Graphic g:walls)
			g.display(gc);
	}
}

public class Map {
	GraphicsContext gc;
	Walls root;
	
	public Map(GraphicsContext gc) {
		root = new Walls(gc);
		
		
		
		renderScene();
	}
	
	void renderScene(){
		root.display();
	}
}
